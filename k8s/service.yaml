apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: fastapi
  ports:
    - name: http
      port: 80          # Service port
      targetPort: 8000  # MUST match containerPort
      protocol: TCP
